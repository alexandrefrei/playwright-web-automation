name: Feature Branch Testing

run-name: >
  ${{
    github.event_name == 'workflow_dispatch' && 
      (inputs.release_version != '' && 
        format('🚀 Playwright Tests - Release: {0} - Env: {1}', inputs.release_version, inputs.environment) 
        || 'Playwright Tests - Manual Run')

    || github.event_name == 'pull_request' &&
        format('PR #{0} - {1}', github.event.number, github.head_ref)

    || github.event_name == 'push' && 
        github.ref_name == 'main' &&
        startsWith(github.event.head_commit.message, 'Merge pull request') &&
        format('{0} → main', github.event.head_commit.message)

    || github.event_name == 'push' &&
        format('Branch - {0}', github.ref_name)

    || 'Playwright Tests'
  }}


on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Test environment where feature is deployed'
        required: true
        type: choice
        options:
          - qa
          - test-environment-1
          - test-environment-2
          - test-environment-3
          - test-environment-4
          - test-environment-5
          - test-environment-6
          - test-environment-7
          - test-environment-8
          - test-environment-9
          - test-environment-10
      test_filter:
        description: 'Path to test file (e.g., tests/block-question-tests/block-question-checkbox.spec.ts)'
        required: true
        type: string
      default_browser:
        description: 'Browser engine for tests'
        required: false
        default: 'chrome'
        type: choice
        options:
          - chrome
          - firefox
          - webkit
      release_version:
        description: 'Optional release version (e.g. 2.2.4)'
        required: false
        type: string


permissions:
  contents: read
  actions: read
  checks: write

jobs:
  feature-test:
    name: 🧪 Execution ${{ github.ref_name }} on ${{ inputs.environment }}
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout branch
        uses: actions/checkout@v4
        with:
          lfs: true
      #
      - name: 📦 Install dependencies
        run: npm install
      #  
      - name: 🖥️ Install Playwright Browsers
        run: |
          BROWSER="${{ inputs.default_browser }}"
          if [ "$BROWSER" == "chrome" ]; then
            echo "Installing chromium for chrome input..."
            npx playwright install chromium --with-deps
          else
          echo "Installing $BROWSER..."
           npx playwright install "$BROWSER" --with-deps
          fi
      #
      - name: 🧾 Run Playwright test
        run: |
          echo "🔍 Starting execution..."
          echo "🌿 Branch: $BRANCH_NAME"
          echo "🌍 Environment: $ENVIRONMENT"
          echo "📄 Test file: $TEST_FILE"
          echo "🌐 Browser: $DEFAULT_BROWSER"

          npx playwright test "$TEST_FILE"
        env:
          BRANCH_NAME: ${{ github.ref_name }}
          TEST_ENVIRONMENT: ${{ inputs.environment }}
          TEST_FILE: ${{ inputs.test_filter }}
          DEFAULT_BROWSER: ${{ inputs.default_browser }}

          # AQUI usamos env no CI para determinar quais sao os valores que precisamos usar no arquivo .env
          # Eh feito espelhamento entre o env do CI com o arquivo .env
      #
      - name: 📤 Upload Playwright Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-${{ github.run_number }}
          path: playwright-report/
          retention-days: 7
      #
      - name: 📤 Upload Test Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-${{ github.run_number }}
          path: test-results/
          retention-days: 7
      #
      - name: 📊 Test Summary
        if: always()
        run: |
          echo "## 🧪 Feature Test Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch:** \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Environment:** \`${{ inputs.environment }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Test File:** \`${{ inputs.test_filter }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Browser:** \`${{ inputs.default_browser }}\`" >> $GITHUB_STEP_SUMMARY
